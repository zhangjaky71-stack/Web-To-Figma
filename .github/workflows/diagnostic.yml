name: Diagnostic (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner sanity
        run: |
          echo "runner-ok"
          node --version
          npm --version
