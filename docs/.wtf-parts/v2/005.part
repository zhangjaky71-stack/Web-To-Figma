
```text
NODE-29 ~ NODE-31
```

目标：

```text
可测试
可发布
可维护
```

---

# 65. V2 Vertical Slice

在所有功能完成前必须先打通一个最小端到端路径。

Fixture：

```html
<section class="hero">
  <img class="cover" />
  <div class="content">
    <h1>Hello</h1>
    <p>World</p>
    <button>Start</button>
  </div>
</section>
```

要求：

```text
Capture
→ Stable IDs
→ Source Tree
→ Render Tree
→ .wtf
→ Figma
→ Hero
   ├ Image
   └ Content [Vertical Auto Layout]
      ├ Heading
      ├ Description
      └ Button
```

然后 Resize：

```text
1440 → 1024
```

检查：

- Content；
- FILL；
- HUG；
- spacing。

---

# 66. Benchmark Fixture V2

## Level 1

基础视觉。

## Level 2

Flex / Grid / Absolute。

## Level 3

真实 Product UI。

## Level 4

响应式：

- desktop/tablet/mobile；
- media；
- container query；
- hidden/show；
- reorder。

## Level 5

复杂：

- table；
- inline；
- Shadow DOM；
- iframe；
- canvas；
- WebGL；
- blend；
- local site；
- virtualized list。

---

# 67. Debuggability

每个 Layout / Render 决策：

```text
decision
confidence
reasons
source evidence
```

例如：

```json
{
  "widthSizing": "FILL",
  "confidence": 0.96,
  "reasons": [
    "authored width:100%",
    "1440→768 snapshots scale with parent",
    "computed flex-grow:1"
  ]
}
```

---

# 68. Error / Diagnostic Domains

统一：

```text
SOURCE
PERMISSION
CAPTURE
DOM
CSS
TEXT
ASSET
RESPONSIVE
LAYOUT
COMPOSITING
FILE
FIGMA
FONT
RENDER
QA
PERFORMANCE
SECURITY
```

示例：

```text
W2F_W_RESPONSIVE_STRUCTURAL_VARIANT
W2F_W_RENDER_WRAPPER_INSERTED
W2F_W_FIGMA_CAPABILITY_DOWNGRADE
W2F_W_BLEND_FALLBACK_PROMOTED
W2F_W_VIRTUAL_LIST_PARTIAL
W2F_W_FONT_SUBSTITUTED
```

---

# 69. 性能目标

建议建立基线：

```text
2k render nodes   → normal
5k                → normal
10k               → chunked
20k               → warning
50k               → design-friendly / section import recommendation
```

最终限值通过 benchmark 调整。

优化方向：

- computed style dedup；
- cascade hash；
- asset hash；
- spatial index；
- batch Figma creation；
- image cache；
- font cache；
- render tree reduction。

---

# 70. Partial Import 与大型页面

`.wtf` 打开后先解析 Outline。

用户可：

```text
Whole Page
Selected Sections
```

减少：

- Figma node 压力；
- 不必要的 raster；
- 导入时间。

---

# 71. 安全

继续坚持：

```text
.wtf = data
```

不是 executable package。

Importer：

- no JS；
- no eval；
- no HTML execution；
- SVG sanitize；
- decompression limits；
- path validation；
- MIME validation；
- checksum。

---

# 72. 隐私

V2 默认：

- no cloud；
- no cookies；
- no local storage；
- no authorization header；
- password redaction；
- form value capture 默认关闭；
- telemetry 默认关闭。

---

# 73. Build Strategy

建议：

```text
W2F Standard
W2F High Fidelity
```

同一 Monorepo。

Chrome manifests：

```text
manifest.standard.json
manifest.high-fidelity.json
```

原因：

`debugger` 权限体验较重，应作为正式产品决策，而不是临时开关。

---

# 74. Browser/Figma Adapter Isolation

Chrome：

```text
BrowserPlatformAdapter
```

Figma：

```text
FigmaPlatformAdapter
```

业务层不大面积直接调用平台 API。

这样未来 API 变化更容易处理。

---

# 75. V2 最终 Definition of Done

V2 RC 必须全部满足：

### Capture

- Full Page；
- Region；
- Online；
- file://；
- Local Folder；
- Standard；
- High Fidelity；
- Text；
- Assets；
- Pixel Reference；
- Responsive snapshots。

### Data

- `.wtf V2`；
- Schema；
- Compatibility；
- Feature flags；
- Checksums；
- Source Tree；
- Render Tree；
- Stable Identity；
- Diagnostics。

### Figma

- Choose；
- Drop while active；
- Partial Import；
- Frames；
- Text；
- Images；
- SVG；
- Paint；
- Auto Layout；
- Grid；
- FILL/HUG/FIXED；
- Constraints；
- min/max；
- Native/Emulated/Raster；
- Rollback。

### Quality

- Visual QA；
- Structure QA；
- Editability QA；
- Responsive QA；
- Determinism QA；
- Performance QA；
- Compatibility Matrix。

### Security

- Archive validation；
- SVG sanitization；
- redaction；
- no executable content；
- privacy documentation。

---

# 76. 后续正式开发执行规则

从现在开始，后续开发不得再以 V1 NODE 编号为准。

唯一执行顺序：

```text
NODE-00
↓
NODE-01
↓
NODE-02
↓
...
↓
NODE-31
```

每个 NODE：

1. 建立本 NODE markdown；
2. 实施代码；
3. 测试；
4. Benchmark（如适用）；
5. 更新状态；
6. Commit；
7. DoD 全部通过；
8. 才进入下一 NODE。

---

# 77. Implementation Status 模板

```md
# W2F Implementation Status

Baseline: V2

| NODE | Name | Status | Tests | Commit |
|---|---|---|---|---|
| 00 | Product Baseline | TODO | - | - |
| 01 | Monorepo | TODO | - | - |

## Current Node

NODE-00

## Blockers

None

## Next

NODE-01
```

---

# 78. Architecture Decision Records

至少建立：

```text
ADR-0001 W2F IR
ADR-0002 Source Tree vs Render Tree
ADR-0003 Stable Identity
ADR-0004 Multi-Viewport Responsive
ADR-0005 Standard vs CDP Capture
ADR-0006 Local First
ADR-0007 Minimal Raster Fallback
ADR-0008 Figma Capability Resolver
ADR-0009 Font Handling
ADR-0010 W2F Compatibility Contract
```

---

# 79. 最终产品定位

W2F 的长期价值不是：

```text
网页截图插件
```

而是：

```text
Web Rendering State
        ↓
Semantic Portable Representation
        ↓
Design Reconstruction
```

核心资产：

```text
1. Capture Engine
2. W2F Source Graph
3. W2F Semantic IR
4. Stable Identity
5. Responsive Engine
6. Render Tree Optimizer
7. Figma Capability Resolver
8. Figma Renderer
9. Visual/Structure QA
```

---

# 80. 下一步

V2 Baseline 已经定义好后，下一步正式进入：

```text
NODE-00 — Product Baseline & Acceptance Contract
```

NODE-00 的目标不是继续讨论架构，而是把：

```text
产品范围
验收指标
P0/P1/P2
Non-goals
质量门槛
Release Gates
```

固化成工程仓库中的正式文档，然后开始 NODE-01 Monorepo。

---

# 81. V2 结论

V2 对 V1 最关键的改动可以浓缩成一句话：

> **不再把网页理解成“一次 DOM 截图”，而是理解成一个带来源身份、CSS 语义、响应式证据、视觉真值和可编辑重建策略的 Web Rendering State。**

最终链路：

```text
Online / Offline Web
        ↓
Source Provider
        ↓
Standard / CDP Capture
        ↓
Source Graph
        ↓
Stable Identity
        ↓
CSS Cascade + Responsive Evidence
        ↓
Semantic IR
        ↓
Render Tree
        ↓
.wtf V2
        ↓
Figma Capability Resolution
        ↓
Native / Emulated / Raster
        ↓
Editable Figma Scene
        ↓
Visual + Structure + Responsive QA
```

这份 V2 作为后续唯一 Baseline，可以直接用于逐 NODE 实际开发。
