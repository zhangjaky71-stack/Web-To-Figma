# `.wtf` Format Acceptance

The documentation migration is accepted only when all export/import package references use `.wtf`, the MIME is `application/x-wtf`, and W2F remains only the product/internal namespace.
