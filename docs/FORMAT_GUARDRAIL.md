# Export Format Guardrail

Do not reintroduce `.w2f` as the portable exported package extension.

Use `.wtf` / `application/x-wtf` in browser export, Figma import, specs, fixtures, tests and docs. Keep `W2F` only as the product/internal namespace.
