# Packages

NODE-01 initializes only `@w2f/shared-utils` as an executable proof package. Planned domain packages from the frozen architecture (schema, IR, capture, responsive, renderer, QA, etc.) are introduced by their owning NODE instead of as empty package shells.
